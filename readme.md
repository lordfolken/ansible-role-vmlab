VMLAB for Debian / Opensuse
===========================

Vmlab that sets up zfs, apt-cacher-ng and libvirt. 
It creates a webserver that hosts preseed/autoyast files. 

Currently Supported OS:
- Debian Stable
- Debian Oldstable
- Opensuse

